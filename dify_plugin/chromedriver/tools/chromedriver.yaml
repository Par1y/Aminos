identity:
  name: "chromedriver"
  author: "par1y"
  label:
    en_US: "chromedriver"
    zh_Hans: "chromedriver"
    pt_BR: "chromedriver"
    ja_JP: "chromedriver"
description:
  human:
    en_US: "Allow agent to control a browser via chromedriver."
    zh_Hans: "允许 agent 使用 chromedriver 控制浏览器。"
    pt_BR: "Permite que o agente controle um navegador via chromedriver."
    ja_JP: "エージェントが chromedriver を介してブラウザを制御できるようにします。"
  llm: "Allow agent to control a browser via chromedriver."
parameters:
  - name: command
    type: string
    required: true
    label:
      en_US: CDP command
      zh_Hans: CDP 命令
      pt_BR: Comando CDP
      ja_JP: CDP コマンド
    human_description:
      en_US: "command send to chromedriver to execute."
      zh_Hans: "发送给 chromedriver 执行的命令。"
      pt_BR: "Comando enviado ao chromedriver para execução."
      ja_JP: "chromedriver に送信して実行するコマンド。"
    llm_description: 'send the cdp command to chromedriver for execution. The command should follow `Chrome Devtools Protocol` format, contain command and arguments, wrapped in json string. Example: `{"cmd":"Page.captureScreenshot","args":{"format": "jpeg"}}`'
    form: llm

extra:
  python:
    source: tools/chromedriver.py
